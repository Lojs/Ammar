# سجل التغييرات (Changelog)

هذا الملف يوثّق أهم التغييرات على مشروع عَمار عبر كل إصدار.

---

## [1.0.0] — 2026-09-19

أول إصدار مستقر وجاهز للاستخدام الفعلي.

### الميزات الأساسية
- إدارة مشاريع متعددة، كل واحد مستقل تمامًا (بدون إجمالي عام يجمعها).
- هيكلة ثلاثية: أقسام ← بنود ← دفعات، مع إنشاء ٣ أقسام افتراضية تلقائيًا لكل مشروع جديد (المواد، المصنعيات، بنود أخرى).
- مبلغ "متفق عليه" اختياري لكل بند لحاله — تفعّله أو تعطّله حسب طبيعة البند.
- **عملة مستقلة لكل مشروع** بدل عملة عامة واحدة لكل الحساب.
- رفع صور الفواتير من الكاميرا أو الاستديو مباشرة أثناء إنشاء الدفعة، مع عارض صور بالحجم الكامل.
- تصدير PDF منسّق للطباعة، يستبعد تلقائيًا أي بند بدون دفعات فعلية.
- تصدير Excel (CSV) بترتيب هرمي احترافي (قسم ← بند ← دفعات) مع إجمالي عام، ويدعم العربي (UTF-8 BOM).
- نسخ احتياطي محلي (تصدير/استيراد ZIP يشمل البيانات والصور).
- نسخ احتياطي سحابي اختياري متوافق مع أي خدمة S3 (AWS، Cloudflare R2، Backblaze B2، MinIO...)، مع جدولة تلقائية يومية وسياسة احتفاظ قابلة للتخصيص.
- تعدد المستخدمين: عزل كامل للبيانات بين الحسابات، لوحة تحكم للأدمن (إضافة/تعطيل/حذف مستخدمين، إعادة تعيين كلمات المرور)، وخيار فتح التسجيل العام لأي زائر.
- ثلاثة أوضاع مظهر (فاتح/داكن/حسب النظام) محفوظة لكل مستخدم.
- تشغيل كامل داخل حاوية Docker واحدة، تدعم x86_64 وARM (بما فيها Raspberry Pi).

### الأمان
- توليد سر JWT عشوائي تلقائيًا عند أول تشغيل بدل الاعتماد على قيمة افتراضية معروفة بالكود.
- عدم الثقة بترويسات البروكسي (X-Forwarded-For / X-Forwarded-Proto) إلا بتفعيل صريح (`TRUST_PROXY=1`)، لمنع تجاوز حد محاولات تسجيل الدخول.
- إصلاح ثغرة Zip Slip في مساري استعادة النسخ الاحتياطية (محلي وسحابي).
- التحقق الفعلي من محتوى كل صورة مرفوعة (بصمة الملف) بدل الاكتفاء بنوع الملف المُعلن من المتصفح.
- معالج أخطاء مركزي يمنع تسريب تفاصيل تقنية داخلية (مسارات ملفات، مكتبات) عند حدوث خطأ.
- تحييد حقن الصيغ (Formula/CSV Injection) بملفات Excel المصدَّرة.
- تعقيم كل النصوص قبل إدخالها بتقارير PDF لمنع XSS.
- انتهاء صلاحية جلسة الدخول تلقائيًا حتى بدون خيار "تذكرني".
- تشغيل حاوية Docker بصلاحيات مستخدم غير-root.
- ترقية مكتبة رفع الملفات (multer) لآخر نسخة مُصلحة أمنيًا.
- حماية من حذف أو تعطيل آخر حساب أدمن بالنظام.
- عزل صارم لبيانات كل مستخدم على مستوى كل استعلام بقاعدة البيانات، مع إرجاع "غير موجود" بدل "ممنوع" عند أي محاولة وصول غير مصرح بها.

### الواجهة وتجربة الاستخدام
- أزرار الإنشاء (قسم جديد، إضافة بند، إنشاء دفعة) بأعلى كل صفحة بدل أسفلها.
- عرض وصف المشروع تحت اسمه مباشرة.
- إضافة صور الفاتورة أثناء إنشاء الدفعة مباشرة، بدون خطوة حفظ منفصلة.
- إمكانية تعديل أو حذف أي مشروع، قسم، بند، أو دفعة بعد إنشائها.
- دعم زر/إيماءة الرجوع بالجوال للتنقل داخل التطبيق بدل الخروج منه.
- قائمة عملات مخصصة بتمرير سلس تضمن ظهور كل الخيارات على كل الأجهزة.
- إغلاق كل النوافذ المنبثقة بزر (×) فقط لتفادي فقدان البيانات بالخطأ.
- إخفاء أسهم الزيادة/النقصان من حقول إدخال المبالغ.

---

## التوافق مع الإصدارات القديمة

قاعدة البيانات تُرحَّل تلقائيًا عند أول تشغيل لهذا الإصدار على بيانات أقدم:
- الأعمدة الجديدة (`has_agreed`, `currency`) تُضاف تلقائيًا.
- كل مشروع موجود مسبقًا يأخذ نفس العملة اللي كانت محددة بإعدادات صاحبه قبل التحديث، فلا يتغيّر أي شيء تراه بمشاريعك الحالية بدون تدخل منك.
